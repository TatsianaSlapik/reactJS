import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders learn react h1", () => {
  /* render(<App />);
  const h1Element = screen.getByText("FIND YOUR MOViE");
  expect(h1Element).toBeInTheDocument();*/
});
